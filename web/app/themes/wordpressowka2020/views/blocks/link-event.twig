{% set img = Image( fields.obrazek ) %}

<div class="wordpressowka-block">
	<div class="wordpressowka-link event" role="link">
		<div class="inner">
			<picture role="img">
			<source
				media="(max-width:880px)"
				type="image/webp"
				data-srcset="{{ img | resize( 880, 450, 'center' ) | towebp }}"
				srcset="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmls='http://www.w3.org/2000/svg' viewBox='0 0 880 450'></svg>") }}"
			/>
			<source
				media="(max-width:880px)"
				data-srcset="{{ img | resize( 880, 450, 'center' ) }}"
				srcset="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 880 450'></svg>") }}"
			/>
			<source
				media="(min-width: 8811px) and (max-width:1023px)"
				type="image/webp"
				data-srcset="{{ img | resize( 1024, 900, 'center' ) | towebp }}"
				srcset="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1024 900'></svg>") }}"

			/>
			<source
				media="(min-width: 8811px) and (max-width:1023px)"
				data-srcset="{{ img | resize( 1024, 900, 'center' ) }}"
				srcset="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 1024 900'></svg>") }}"
			/>
			<source
				media="(min-width: 1024px)"
				type="image/webp"
				data-srcset="{{ img | resize( 208, 270, 'center' ) | towebp }}"
				srcset="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 208 270'></svg>") }}"
			/>
			<img
				data-src="{{ img | resize( 208, 270, 'center' ) }}"
				src="data:image/svg+xml,{{ fn('rawurlencode', "<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 208 270'></svg>") }}"
				class="lazyload"
				alt="{{ img.title }}"
			/>
		</picture>
			<div>
				{% if fields.header %}
					<h2>{{ fields.header }}</h2>
				{% endif %}
				<div>
					{{ fields.opis }}
					<a href="{{ fields.link.url }}" title="{{ fields.link.title }}" class="link" target="_blank" rel="noopener">
						{{ fields.link.title }}
					</a>
				</div>
			</div>
		</div>
	</div>
</div>
