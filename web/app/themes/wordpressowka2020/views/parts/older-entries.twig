<a class="edition {{ extra_class }}" href="{{ post.link }}">
	<div>
		<time>{{ post.date('d F Y') }}</time>
		<p>{{ post.title }}</p>
		<h2>
			<span>{{ __( 'Tematy:', 'sasquatch' ) }}</span>
			{{ post.tags() | join(', ')}}
		</h2>
	</div>
</a>
